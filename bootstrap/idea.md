Repo Bootstrapper (Universal)

Command:

bootstrap .

It detects:

Node → runs npm install

Python → creates venv + installs

Rust → cargo build

Go → go mod tidy

Docker → builds container

Basically:

One command to set up ANY repo.

Very practical. Very GitHub-friendly.
